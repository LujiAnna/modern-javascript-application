export let city = (cityElement) => {
  let city ='';
  return city=cityElement.value;
  //  or dont use `return` but also remove `{}`
  // read more: https://stackoverflow.com/questions/45754957/why-doesnt-my-arrow-function-return-a-value
}